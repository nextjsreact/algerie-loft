// Middleware temporairement désactivé pour tester les routes de base
export function middleware() {
  console.log('Middleware désactivé - test des routes');
}

export const config = {
  matcher: []
};