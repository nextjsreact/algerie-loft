/**
 * Next.js instrumentation file
 * Temporarily completely disabled to avoid performance monitoring errors
 */

export async function register() {
  // Completely disabled for clean console during portal testing
  console.log('Instrumentation disabled - clean console mode');
}